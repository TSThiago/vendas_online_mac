import { SafeAreaView, Text, View } from "react-native";
import Login from "./modules/login";

const App = () => {
return (
    <SafeAreaView>
        <Login/>
    </SafeAreaView>
)
};

export default App;